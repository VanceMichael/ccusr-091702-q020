# 领域约定

青少年书目审议档案库围绕候选书目、匿名评议、利益回避和发布更正保存业务事实。主要对象为征集版本、候选快照、评审人、利益关系、匿名意见、复核轮次、发布版本、访问凭证。

必须长期遵守以下约束：截止后候选快照不可变；存在利益冲突时自动回避；揭盲前不得侧漏身份；发布后的变化通过关联新版本表达。

时间以带时区的 ISO 8601 字符串交换，服务内部保存 UTC；外部写请求使用调用方提供的幂等键；审计记录采用追加方式保存，不通过覆盖来隐藏历史。